{
  "name": "[forge] Evidence collector unavailable",
  "type": "log alert",
  "query": "logs(\"(env:prod OR @env:prod) \\\"event=recommendation.evidence.collector\\\"\").index(\"*\").rollup(\"count\").last(\"5m\") >= 5",
  "message": "Operational counts are incomplete. Check the shared RECOMMENDATION_EVIDENCE_REDIS_URL and log pipeline; missing counters are unknown.\n\nRunbook: docs/operations/recommendation-evidence-transport.md. Configure the approved notification destination before installation.",
  "tags": ["team:forge", "env:prod", "feature:feat-464"],
  "priority": 2,
  "options": {
    "thresholds": {
      "critical": 5
    },
    "notify_no_data": false,
    "evaluation_delay": 60,
    "renotify_interval": 60,
    "enable_logs_sample": false,
    "include_tags": true
  }
}
