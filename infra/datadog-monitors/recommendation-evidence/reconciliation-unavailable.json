{
  "name": "[forge] Profile reconciliation batch unavailable",
  "type": "log alert",
  "query": "logs(\"service:forge-admin env:prod \\\"event=recommendation.reconciliation.heartbeat\\\" \\\"outcome=unavailable\\\"\").index(\"*\").rollup(\"count\").last(\"5m\") >= 1",
  "message": "Inspect durable scheduler and authorized current-pointer audit. Expected workflow step/wait suspension is not a batch failure.\n\nRunbook: docs/operations/recommendation-evidence-transport.md. Configure the approved notification destination before installation.",
  "tags": ["team:forge", "env:prod", "feature:feat-464"],
  "priority": 2,
  "options": {
    "thresholds": {
      "critical": 1
    },
    "notify_no_data": false,
    "evaluation_delay": 60,
    "renotify_interval": 60,
    "enable_logs_sample": false,
    "include_tags": true
  }
}
