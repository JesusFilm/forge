# Client Agent Guide

Scope: `packages/client`.

## Rules

- Generation must be deterministic.
- Do not hand-edit `src/generated/*`.
- Regenerate when schema changes: `pnpm run generate` from repo root.
