// Bump this constant on every template-affecting change (layout, fonts,
// primitives, caption rendering, schema rendering semantics). It is recorded
// in render meta artifacts so outputs can be traced to a composition
// revision — package.json versions are dead 0.0.1 stamps in this repo.
export const COMPOSITIONS_VERSION = "2026-06-11.1"
