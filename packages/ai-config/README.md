# packages/ai-config

Versioned AI configuration artifacts.

- `prompts/` prompt templates
- `policies/` guardrails and safety policy
- `evals/` quality evaluation configs

These files are inputs to orchestration, not runtime business logic.
