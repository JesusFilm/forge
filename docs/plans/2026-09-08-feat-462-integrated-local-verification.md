# Studio integrated local release verification

Root authorized unpaid local verification after reviewed calendar integration.
Fixed review baseline `1dd20216` includes the calendar docs-only followup and
has exact reviewed root tree `e94ed4b2503533845d7f27298ae581bc308d7bf1`. Retirement is included once.
Feature 462 remains in progress; this plan does not authorize deployment or
provider operations.

1. Replay the complete migration chain through calendar 0092 against a fresh
   owned Postgres cluster on verified loopback port 55462, database
   `forge_studio_462_test`. Use a network namespace and rootfs TMPDIR before
   processes/tests; no default/shared Redis or Postgres access.
2. Generate Prisma, Admin SDL and typed GraphQL client from this integrated source
   and inspect drift. Check the combined migration/schema boundary and focused
   database contracts only where this adds evidence beyond predecessor runs.
3. Verify both Admin workflow build verifiers are preserved and invoked. Reuse
   exact-tree predecessor build evidence where it already proves the final source;
   do not repeat builds or all suites without an identified gap. Verify current
   Studio/retired route and shared worker composition boundaries.
4. Map every original brief/plan acceptance requirement to authoritative existing
   evidence, substrate and remaining gap. Preserve historical failures and
   inconclusive performance. Complete a receiver-first release/canary/rollback
   runbook with exact controls and named operator/provider dependencies.
5. Independently review Standards and Spec from the fixed integrated baseline,
   run necessary formatting/hooks, and commit only this local verification slice.

No OCI builder installation or image execution, provider/account check, paid call,
asset upload, shared DB/Redis operation, archive/data migration or deletion,
push/PR/merge/deployment is authorized. Disposable fixtures are not original assets.
All paid batches remain closed. The subsequently authorized source fixes below
receive bounded failure-first coverage and review before commitment.

## Authorized bounded correction

Fresh replay exposed20 Studio FK update-action and three default mismatches. Root
authorized aligning only Studio Prisma metadata with the applied immutable SQL,
retaining all migrations and red drift. Explicit NoAction/default declarations
remove Studio diff; generated declarations stay identical. Targeted schema and
publication adapter tests plus regenerated Admin/typed-client types verify this
change. No unrelated inherited drift is repaired or applied.

## Authorized missing disable controls

Root explicitly released two independent default-off Admin admission controls.
`STUDIO_PRODUCTION_ENABLED` gates new attempts/experiments/runs and paid/render/Mux
claims after existing outcomes; `STUDIO_PUBLICATION_ENABLED` gates new manual and
stored scheduled submissions after receipt lookup and before consumption/visibility.
No new registry/role authority. Preserve manual editing/planning/read/export, late
retention/settlement, unpublish/delivery and exact accepted retries. The runbook
records process-start activation/drain limitations and expiry without substitution.
Real owned-DB regressions cover canonical denial, zero-budget dispatch, continued
settlement and two-connection lock-wait revalidation. Native/provider/image/deployed
proof remains external. Fixed review baseline remains1dd20216 for this whole slice.

## 2026-09-09 bounded VM acceptance documentation

Approved ordered root alignment:5f4cb768,5a9505cc,a144ac95,f9e7165c,a13dace2,
excluding equivalent bb6ed63f/d6d1b59b. Clean resulting local base
`08c69c4ebde7be71f2c6751bf64e14fa746c78d8` has reviewed tree
`a58514a14a9c4787807509a6621223a0db23dfbc`.

Edit only this plan,462 acceptance matrix/README and their manifest entries, and
`docs/runbooks/studio-release-canary-and-rollback.md`. Coordinate the runbook with
460; it owns VM ops, hosted CI/release tooling and its new validation/plan. Record
exact current VM/worker milestones and evidence limits, replace superseded receiver
model, and prepare minimal named external actions. Preserve all historical logs and
qualifications. Independent Standards/Spec docs review and normal hooks follow; no
suites/builds/audits/external operations. Full release stays incomplete.
