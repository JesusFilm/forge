# Studio validation PR cleanup

Scope: feat-462 remains in progress. Respond to the user request to remove excessive validation material from PR #2205 before merging. Preserve a verified archive of all added validation bytes, keep summaries and actual automated-test fixtures, and remove raw artifacts from the final diff. Do not change runtime behavior, test assertions, historic outcomes, controls or release acceptance.

Validation: verify archive extraction hashes; audit source and script consumers; verify every retained fixture against the original bytes; review deletions and documentation; run formatting checks. No provider, runtime, performance or database reruns are required for unchanged fixture bytes. Review against b0b690cbe487b4d35884f91d28f9af20f43c6a4b before committing.

Additional user-requested cleanup: consolidate the 23 unmerged Admin Studio migrations into `0094_shorts` while preserving existing main migrations. Prove schema equivalence on fresh isolated databases; do not alter existing fixture or deployed migration ledgers. Mastra's separate database migration remains separate. Remove one-off proof/recovery scripts and their package command; keep the runtime codec verifier and maintained CI/build checks. The pending Watch scope decision is independent and its staged changes must not enter this cleanup commit.

Result: seven one-off proof/recovery scripts removed; the retained worker typecheck passes. Original 114-migration and consolidated 92-migration histories both replayed in fresh isolated PostgreSQL 18 databases, producing identical normalized full schemas. All existing main migration bytes are unchanged. The single migration preserves original SQL ordering rather than rewriting functions or triggers. Existing pre-merge disposable Studio fixture databases keep their historical ledgers; use a fresh database for this consolidated branch instead of relabeling applied migrations.
