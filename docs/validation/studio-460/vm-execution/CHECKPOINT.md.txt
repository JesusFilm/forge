# VM execution checkpoint — 2026-09-09

Working implementation, not release acceptance. Fixed source base5f460b6d/rootf9e7165c. Production/provider access remains disabled. No image publication or deployment workflow has run.

Canonical assignment: migration0093 applies after fresh owned replay through0092. Nullable pool/worker/dispatch extend immutable leases. assignment-red.log is5failed at missing method; assignment-green-candidate.log is4passed/1failed (fixture configuration captured at import); assignment-lease-green.log is14passed/1failed (raw timestamp timezone bug); assignment-lease-utc-green.log is15passed. assignment-lookup-red.log fails missing lookup; assignment-lookup-green.log is7passed. Admin tsc inadmin-types-wip.log passed before subsequent lookup/RPC/retention additions; it is not final-head type evidence.

Journal: journal-review-red.log reproduces malformed validJSON admission, failed temporary-write evidence deletion, and expiry during persistence. journal-review-green.log is a FAILED intermediate shadowing typo, not green. journal-review-corrected-green.log passes8. Spec follow-up found these three addressed. Full client/launcher integration remains separate.

Actual VM claim: poll-vm-first-claim.log failed after canonical lease commit because lookup response included extra fields; immutable DTO narrowed. poll-vm-retry-claim.log recovers the exact persisted dispatch and lease; poll-canonical-lease.log confirms one generation. Loopback-only reverse SSH listener inpoll-vm-listener.log. Fixture startup failures (CJS top-levelawait, ESM named import, missing bundler path, missing Manager fixture config) remain in their original logs.

Actual connected text render: outbound-render-first.log is FAILED at retention, after successful render and independent verification. Manager broker was imported under Admin tsconfig aliases and lacked its local transfer endpoint. Separately bundled Manager helper plus actual canonical StudioTransferService upload route fixed the fixture. poll-vm-retention-recovery.log resends saved verified output under the original still-active lease; no new render/start/deadline. Canonical final state inpoll-canonical-finish.log: SUCCEEDED/COMPLETED, generation1, retained3, admittedexecution1. This is recovered roundtrip evidence, not an uninterrupted60s all-phase acceptance result. The recovered transfer used a separate bounded295ms request after the original short execution scope retired.

Output ../outbound-render.mp4, SHA3299965b7682aaca7f9ad80c67891ec3f9c2f75d31d53f41642ab080b96af1ec,47,771bytes; proof inoutbound-codec-proof.json. H264320x180/30fps/30frames/1000ms; stereoAAC48kHz/1045.333ms; independent decode true. ../outbound-render.png visibly reads VM outbound fixture. This canonical text-only fixture complements, not replaces, earlier first-render source/custom/caption/audio proof. Neither is paid generation.

Actual bounded membership inoutbound-membership.json/summary:154samples,32processes including both guards,Node,Chromium,ffmpeg/ffprobe and trusted host controller/client processes. All sampled memberships were under the exact job ancestor with2CPU/2GiB/no-swap/128tasks. memoryPeak179798016,CPUusage3350270microseconds,noOOM/PIDmax events. Sampling is observation, not proof that every ephemeral PID was captured. All sampled original PID/starttimes absent and aggregate cgroup absent inoutbound-process-retirement.log. Earlier first-render's zero-process sample is not upgraded retrospectively.

watchdog-removed-slice-gate.log proves active exact slice accepted, removed slice refused before start with no marker, recreated inode refused. It does not make removing a slice during an unresolved Docker call safe; production launcher must pin it until exact runtime reconciliation, retaining frozen quarantine rather than reopening capacity prematurely.

Next: finish production host launcher/outbound scoped gateway and packaging, cancellation/reassignment during transfers, ambiguous runtime/restart fencing, adversarial timeout/OOM/escape and fresh-instance recovery, effective signed profile/lease/delivery consistency, update/drain/rollback scripts, final focused validation/reviews/hooks. No duplicate historical full-length render or broad performance rechecks requested.
