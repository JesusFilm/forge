> Historical validation summary. Raw artifact paths and original checksum inventories below refer to the [preserved archive](../../STUDIO-ARCHIVE.md); they are not a current checkout file inventory. Test fixtures remain in Git.

# Standards review — Studio replay and deadlines

Reviewed the working-tree diff against `a319a068fc2d79684c1ad1824387c2b8a45ca0dc`, including untracked run-budget and execution tests and `dispatch_guard_v2.py`. Prior paid evidence and earlier feature scope were not re-audited.

No actionable documented-standard violations or consequential baseline-smell findings.

The change keeps canonical authority in Admin, native execution admission in Mastra, and shared timing constants runtime-neutral. Typed validation feedback remains available for repair turns; stalled tool iterations fail without a replay. The shared monotonic budget reserves terminal-write time, propagates cancellation to tools, and retains first-abort telemetry. Terminal persistence destroys timed-out connections and does not retry an uncertain write. The evaluation guard requires a new versioned manifest and protects existing ledgers before adding its per-run request-digest constraint.

Reviewed source and regression assertions only. No tests, provider calls, services, or database mutations were performed. Suite completion and timing evidence remain the implementing agent's responsibility.

Final follow-up: reviewed the Manager deadline/first-caller-reason assertion, retention of an earlier proposal when a later tool iteration stalls, seven V2 guard cases, and clearing the generation timer when settlement begins. These additions preserve the reviewed boundaries; no new Standards findings. Reported suite results were not independently rerun.
