import ts from 'typescript';import vm from 'node:vm';import {readFileSync,writeFileSync} from 'node:fs';import {createHash} from 'node:crypto';import assert from 'node:assert/strict';
const source=readFileSync('apps/mastra/src/services/studio-authoring/runtime.ts','utf8'),snippet=source.slice(source.indexOf('function streaming('));
const javascript=ts.transpileModule(snippet,{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.None}}).outputText+';globalThis.testStreaming=streaming;';
const results=[];
for(const scenario of ['native-deadline','caller-cancel']){let callback:()=>void=()=>{},clock=0,agentSignal:AbortSignal|undefined;const events:any[]=[],caller=new AbortController();const sandbox:any={ReadableStream,Response,TextEncoder,AbortController,Date,console,setTimeout:(f:()=>void,delay:number)=>{assert.equal(delay,90000);callback=f;events.push({kind:'native-timer-created',at:clock,delay});return 1},clearTimeout:()=>{},streamStudioAgent:({signal}:any)=>{agentSignal=signal;events.push({kind:'SDK-abortSignal-received',at:clock});return new Promise((_resolve,reject)=>signal.addEventListener('abort',()=>{events.push({kind:'SDK-upstream-abort',at:clock,name:signal.reason?.name});reject(signal.reason)}));}};
vm.runInNewContext(javascript,sandbox);const response=sandbox.testStreaming({}, {},'fixture',{finish:async(_id:string,status:string)=>events.push({kind:'native-finish',at:clock,status})},caller.signal,'unpaid-fixture');
clock=75000;events.push({kind:'fourth-request-start-fixture',at:clock});clock=scenario==='native-deadline'?90000:80000;if(scenario==='native-deadline'){events.push({kind:'explicit-native-timer-fired',at:clock});callback();}else caller.abort();const body=await response.text();assert.equal(agentSignal?.aborted,true);if(scenario==='native-deadline')assert.match(body,/Studio generation failed/);else assert.equal(body,'');assert.doesNotMatch(body,/"done"/);assert.equal(caller.signal.aborted,scenario==='caller-cancel');results.push({scenario,events,body,syntheticClock:true,externalRequests:0});}
writeFileSync('/tmp/forge-studio-458-prep/model-capability-comparison/binding/live-adapter/live/deadline-diagnosis.json',JSON.stringify({sourceSHA256:createHash('sha256').update(source).digest('hex'),unchangedExtractedStreamingFunction:true,tests:results},null,2));console.log('Two source-extracted native deadline/caller-cancel tests passed; simulated clock, no providers or DB.');
