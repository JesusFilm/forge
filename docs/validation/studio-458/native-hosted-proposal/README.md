> Historical validation summary. Raw artifact paths and original checksum inventories below refer to the [preserved archive](../../STUDIO-ARCHIVE.md); they are not a current checkout file inventory. Test fixtures remain in Git.

# Native-hosted comparison proposal and unpaid guard proof

No paid dispatch is authorized by these files. `proposal.body` is the immutable
original (SHA25616c4a503f47d9690122c17b39714b374cfd1f999440b8b11d3d28c7e56203702).
The real native SDK's initial request was130,673UTF-8bytes; adding4,096framing
exceeded its128,000allowance. It was rejected before network and is preserved as
`guard-evidence/original-rejected-request.body`. No paid request was retried.

`corrected-proposal.body` and `tool-surface-clarification.body` are superseded,
unexecuted correction drafts. The coordinator required a read/proposal-only
execution restriction instead of broadening permission to capture/register.
The current proposal is `corrected-readonly-proposal.body`, SHA256
010140f392b5f95dcab2416b8cae6272eb8841d13917cf64656d68149f722500.
It retains the exact two projects, versioned editorial packs, messages, native
snapshot, model, output and step/request ceilings. The conservative input allowance
is200,000per request; ten times(200000×$0.75/M+4096×$4.50/M)=$1.68432standard usage,
within the proposed$2authorization ceiling. Neither number is an actual charge.

Current primary [model rates](https://openrouter.ai/openai/gpt-5.4-mini/api) and
[provider routing rules](https://openrouter.ai/docs/guides/routing/provider-selection)
were checked, followed by read-only configured-key and exact OpenAI endpoint GETs.
Both returned200; the endpoint reported$0.75/$4.50per million input/output tokens.
No key/subscription/credit setting changed. Sanitized results are retained.

The actual production Manager UI admitted two native dry runs through canonical
Admin and the authoritative native PostgresStore. Four dynamic SDK requests per
run passed the persistent guard, with actual serialized+framing bounds134,769to
148,270and4,096output on every request. System bytes and native agent/block/effective
identity matched the proposal. The SDK received synthetic streamed tool calls;
no request reached a paid endpoint. Both runs finished and page errors were empty.
This is transport/guard evidence, not creative quality or provider proof.

The task-local execution-boundary wrapper preserves all shipped tool definitions
but rejects capture, upload/registration, project edits, media/provider/render/
publish actions before forwarding to Admin. Only assets,packs,pack,source-preview,
search,asset and validate-proposal are forwarded. readProject remains a pinned
in-memory read; proposeEdits validates and emits without applying. Boundary tests
prove denied actions produce zero forwarded calls. This evaluation restriction is
not a production implementation change or capture/registration acceptance proof.

Six guard tests cover the two-run/ten-request global cap, five requests per run,
full serialized UTF-8/framing and4096output settings, exact frozen admission,
concurrent claim losers, owner-only completion, exhausted bounds, oversized dynamic
tool turns, altered proposals, replacement admissions and ambiguous restarts.
SQLite uses an immediate transaction and FULL synchronous durability: reservations
commit before network. Pending or ambiguous calls cannot be replayed or reset by
restarting a process. Dry ledgers are explicitly separate from any eventual paid
batch ledger; paid state must never be deleted or renamed to regain allowance.

The first browser harness failure happened before a request; the next correctly
hit the oversized guard but CDP could not retrieve an already-consumed stream.
The final harness observes a clone of the real browser fetch response while the
application consumes its original stream. Existing failures and corrected logs
remain separate. The original132recovered files and earlier four paid requests
were not changed.
