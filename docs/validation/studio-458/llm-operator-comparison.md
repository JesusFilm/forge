> Historical validation summary. Raw artifact paths and original checksum inventories below refer to the [preserved archive](../STUDIO-ARCHIVE.md); they are not a current checkout file inventory. Test fixtures remain in Git.

# Fixed saved-case operator comparison

Four authorized requests returned HTTP 200 from OpenAI through OpenRouter. Exact outbound requests, responses, generation IDs, duration and returned usage/cost are retained. No retries or extra requests were made. These are real provider outputs, not mocked quality evidence. The batch is insufficient to establish the full product acceptance criteria.

## Findings requiring implementation

- Generation case ch19-seq102 labels a long doctrinal conclusion as `settle` and places event recap in `bridge`. The recovered brief uses a short settling line and a source-grounded causal bridge. Generic role labels did not preserve those semantics. Its composition is prose, not validated canonical operations, and its quality verdicts are self-assessments.
- Generation case ch31-seq104 emits bracketed role labels inside one speech string, inconsistent `[Reflect]`/`[Reflection]` labels, and only a short moral summary. It omits the source's developed argument and concrete care details. It is not acceptable structured speech/composition output.
- Both QA calls mark bridge/settle as pass by interpreting a conclusion or thematic transition as equivalent. The saved fixture did not include explicit complete timeline settle identity. That finding is overconfident: role coverage requires deterministic inspection of the admitted effective script, not semantic guessing from prose.
- The QA responses treat an exhortative pastoral register as sufficient voice QA, without applying the recovered command/appeal/third-party/self-echo/opening-recap/scripture-echo rubric. Those criteria need explicit editable native defaults and inspectable findings.
- Missing historic media and provenance stay `not_checked`. The generation outputs acknowledge missing source previews. No actual source footage, audio audition, provider narration identity or historical-equivalence claim follows from these text responses.

Implementation consequence: native instructions must define bridge/settle behavior and the recovered writing/QA rubric, while typed proposal validation requires individual exact speech roles and deterministic source availability/approval checks. Model QA cannot substitute for human approval or deterministically verified identity. Do not rerun this paid batch without new authorization.
