# Corrected native creative comparison proposal

Proposal only. No paid request, draft save, activation, canonical admission or
project mutation occurred. Repository tree remains unchanged. Both closed batches
and all 235 existing evidence manifest entries were reverified unchanged.

Review `proposal.body` (SHA-256
`acc28d6896a79578906d57a299895dc20e3dd3952d992b306af861def4578719`).
It retains the exact two saved-case messages, r1 projects and versioned Content
Packs. The referenced editorial source bytes are copied as source-\*.body and their
registered digests were checked. No canonical historic footage/subtitle evidence
is available; those checks remain not_checked.

`proposed-agent.body`, `instructions.diff` and `proposed-effective.body` make the
calibration inspectable. The installed native renderer calculates effective digest
`9afe8339baaac0b7b458d17a73f43da8f6edb59c47154e4a132cbc1b7e0a521b`
for 9,113 UTF-8 bytes. Agent instruction snapshot digest is
`bda39c132d881151c66319dda9bbb8ad3987fa805ec07ba45dba82731169222d`.
The current source block remains unchanged to isolate the creative correction.
These are calculated proposed bytes, not a claim that a new native version exists.

The proposal describes save-draft, compare, explicit activation, native freeze and
signed admission binding. Native generated IDs must be recorded in a separate
bound proposal and checked against these exact bytes before any paid release.
Never fill null IDs by guessing or silently overwrite this proposal. Existing
native active versions remain unchanged; inspect current-active-frozen.json.

The reviewed guard limits remain two runs, five dynamic requests each, ten total,
200,000 serialized UTF-8 bytes plus framing allowance, and 4,096 maximum output
per request. USD 1.68432 is the conditional standard-usage bound at the retained
OpenAI route max prices, within a proposed USD 2 ceiling. Current read-only
route/key/rate checks and a fresh unpaid guard/native dry validation remain
pre-dispatch requirements. No fresh provider capability/rate check was made here.
Returned actual cost remains unknown before execution.

The eight retained request envelopes still fit after offline system replacement
(maximum 165,059 including framing). This is not fresh dynamic/native proof.
Every future actual tool turn must independently pass the guard. The proposed
LIVE ledger is distinct and does not yet exist; neither closed ledger was reused.
The source-read/proposal execution allowlist remains mandatory, even though the
shipped native tool schemas also describe capture and registration.

Evaluation distinguishes canonical role validity from editorial quality, preserves
ch19's unsupported depth/echo passes and ch31's actual rejected role claims, and
requires inspectable proposed speech and preview. Because the same cases informed
the editable calibration, this comparison can show regression improvement, not
blind generalization. No extra call is permitted if quality fails.

Service cleanup: none. `service-inventory.json` records task-owned listeners and
working directories. Existing Admin/Manager/native fixture/preview/PG/Redis
services remain available; paid port 4189 remains stopped. No process, DB, media
file, credential or account configuration was removed or changed. Any later
cleanup must record exact owned processes before stopping them and preserve PG,
asset bytes, baseline originals and both closed ledgers.
