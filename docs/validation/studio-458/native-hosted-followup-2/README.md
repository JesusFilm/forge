# Closed native-hosted follow-up 2

The authorized batch is CLOSED: two cases, seven requests, USD 0.0791718
provider-reported cost, no retries, no apply or media. See
[live/comparison.md](live/comparison.md) for the evidence-based disposition and
[live/summary.json](live/summary.json) for exact totals. ch31 has an accepted
proposal/preview; ch19 fails canonical validation. Creative acceptance remains
incomplete, despite specific same-case improvements.

The root preparation files and binding/ subtree are historical proposal and unpaid
validation records. Their proposed/not-authorized labels describe those earlier
stages; live/ contains the subsequent explicit authorization and closed outcome.
Raw body/JSON bytes are unchanged. Executed script evidence uses .txt suffixes to
prevent formatter changes. Guard input scratch files remain in the external
runtime; exact outbound and tool/stream bytes and sanitized ledger results are
retained here. No closed ledger is reset or reused.

A narrowly scoped unpaid property-validation feedback correction follows as a
separate code commit. It must preserve strict schema and role coverage validation.
