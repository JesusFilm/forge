# Native-hosted saved-case comparison

The authorized batch is closed: two runs, eight requests, no retries or replacements.
Provider-reported total cost is USD 0.0972243, distinct from the USD 2 ceiling and
USD 1.68432 conservative standard-usage bound. Both native runs completed with
zero page errors. No proposal was applied, no speech approved and no media generated.

## ch19-seq102: accepted proposal, incomplete creative quality

`case0-accepted-proposals.json` contains one canonical proposal. The actual built
UI rendered its proposed composition (`live-ch19-seq102.png`). Canonical set-text
updates effective speech as well as display text, so the retained script is real
proposed speech, not a display-only workaround. Three individual roles remain
inspectable. The settle is short and distinct; the bridge provides a connection
between points rather than bracketed role labels in one unstructured string.

The content remains insufficient. It dwells on the threefold Gospel attestation
and disciple fear but underdevelops the excerpt's distinction between possessing
faith and keeping it ready, its moderation of expectations of weak believers,
and its practical conclusion. The proposed text is shorter than the requested
150–220-word target. Its depth and coherence passes therefore overstate coverage.
The bridge carries much of the body, while the actual ready-faith conclusion is
compressed into the settle. Voice QA labels a positive personal echo as a pass
without clearly testing unwanted repetition, and treats third-party address as
not_checked rather than explaining the intended audience. These findings require
human editorial review; the model's pass labels are not acceptance.

## ch31-seq104: no accepted composition

`paid-1-1-response-proposal-0.json` and
`paid-1-2-response-proposal-0.json` retain the two rejected tool proposals. The
first also shortened total duration without moving all item starts. The second
restored duration, but both falsely claimed hook/conclusion/scripture_echo roles
on items whose actual speech roles were reflection/bridge/settle. Typed validation
correctly rejected those claims. Semantic qualities and stylistic observations
belong in QA findings; they do not create new timeline speech-role identities.

The native tool exposed only a generic rejection, so the model did not receive
which role fact was wrong. It ended with retained prose and honestly acknowledged
that no accepted edit existed. `hosted-live-evidence.json` contains this exact
response and the built UI's “No structured composition was proposed” state;
`live-ch31-seq104.png` shows it. No composition preview was possible for this case.
The prose preserves more concrete care details than the earlier fixed request,
but its self-reported word-count/timing and QA passes remain unverified and do not
replace canonical operations or human judgment.

## Boundaries and next unpaid work

Both cases used the exact admitted native agent/block/effective snapshot,
prepared project revisions and immutable editorial Content Packs. Absent canonical
footage/subtitles remain explicitly not_checked. Original historic narration is
not equivalent. Raw streamed provider bodies, request IDs, usage, elapsed time,
bounded reservations and the actual live adapter are retained alongside this file.
The dedicated LIVE SQLite ledger remains at the path recorded in `summary.json`;
it must never be reset to regain allowance.

The next correction is actionable, bounded typed validation feedback containing
actual admitted timeline-role facts and stable codes. It must preserve source and
caller authority, reject false coverage, and avoid imposing universal roles or
content arrangements. This batch is a regression fixture for that unpaid fix.
Creative acceptance remains partial/failing; no further paid calls are authorized.
