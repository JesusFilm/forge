> Historical validation summary. Raw artifact paths and original checksum inventories below refer to the [preserved archive](../../STUDIO-ARCHIVE.md); they are not a current checkout file inventory. Test fixtures remain in Git.

# Browser validation in progress

These are intermediate operator checks, not final release acceptance or paid
quality evidence. The Manager production build, canonical Admin services and
task-owned PostgreSQL run on isolated loopback services. External ElevenLabs
requests are intercepted in the Manager process and return a locally generated
two-second tone. The native Mastra runtime uses its real pinned instruction and
execution stores with a deterministic model. No provider credits are consumed.

All corrected contexts use `serviceWorkers: "allow"`, including baseline and
final loading samples. Page errors are collected without filtering. The opaque
`sandbox="allow-scripts"` remains unchanged. See `../preview-diagnosis/` for the
minimal Playwright injection red/green attribution.

## Observed workflow boundaries

- `full-workflow-1.log`: generation was correctly blocked until the operator
  activated native instructions.
- `full-workflow-3.log`: admission succeeded, but the isolated fixture lacked the
  existing Mastra `004-studio-agent-execution.sql` metadata migration. That
  migration was applied only to the task database; the failed attempt was not
  replayed.
- `full-workflow-4.log`: generation completed. The test's exact accessible label
  for the populated attempt selector was wrong; the harness was corrected to use
  its actual combobox name.
- `full-workflow-7.log`: repeated aborted harness contexts exhausted the preview
  host's eight-session bound. The task-owned host was restarted. Its bounds and
  sandbox were not changed.
- `full-workflow-8.log`: the proposed iframe was ready, but its missing positioned
  canvas container intercepted Apply clicks. The application now gives the
  proposed composition a bounded aspect-ratio canvas.
- `full-workflow-9.log`: retained proposal preview and Apply succeeded, followed
  by music estimate, explicit confirmation, two retained candidates, actual
  browser audio playback and selection. Page errors were empty. This model
  fixture proposed a title change, so this is transport/retention evidence, not
  script-quality acceptance.
- `voice-workflow-1.log`: three previews, audition, selection and registration
  executed. The harness incorrectly rejected an empty alert region. Both the
  design and selected registration ledger calls were `COMPLETED`.
- `voice-retained-check.log`: reloaded those retained results, played the selected
  candidate and inspected provenance, with zero page errors. It records the empty
  alert text explicitly; nonempty alert messages still fail the harness. No
  design or registration request was repeated in this check.

Intermediate screenshots expose the state of the UI at those boundaries. Run
names, candidate names, cost wording, target selection and sticky-header polish
are being refined before the final complete workflow. The next fixture proposes
actual reflection/bridge/settle speech and composition operations, preserving
`not_checked` source fidelity where original source footage is unavailable.

The `.mjs.txt` files preserve the task harnesses. Preview capability URL paths are
redacted in copied logs; original local logs remain untouched in the task's
temporary evidence directory. No credentials are included.
