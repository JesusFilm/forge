# Built local orchestration evidence

Manager build 4 and Admin build 1, isolated Studio 458 database, opaque preview sandbox, normal page-error observation and Playwright `serviceWorkers: allow`.

The native runtime uses its real frozen instruction/admission/execution path with a deterministic language-model fixture. ElevenLabs HTTP is intercepted locally with deterministic audio. No paid provider or creative-quality acceptance is claimed.

The release workflow exercised proposed script/composition preview and application, music candidate audition/selection, and voice candidate audition/selection/registration. The batch workflow exercised two explicit targets and approved speech requests of 3, then 0 after a font edit, then exactly 1 after a bridge edit. Observed request texts matched reviewed text. Both recorded zero page errors.

These captures precede the final expanded-provenance wrapping and source-preview changes; later final UI evidence is recorded separately. The successful 3/0/1 proof is preserved without repeating these requests.
