> Historical validation summary. Raw artifact paths and original checksum inventories below refer to the [preserved archive](../STUDIO-ARCHIVE.md); they are not a current checkout file inventory. Test fixtures remain in Git.

# Reviewed Studio/main merge handoff

The local integration is ready for a two-parent merge commit and root fast-forward. This does not complete feat-462 or authorize release execution.

- First parent: `3ee2451a8ee717fe090890cc03446ee5d2bf6a01` (reviewed Studio/root).
- Second parent: `1ce69015e0e2fe7273e092fa14afdfaea5a06cc3` (exact fetched main).
- Owned branch: `codex/studio-main-integration` in `/home/tataihono/.local/share/forge/studio-462-main-integration/checkout`.
- Original checkout and all prior evidence are preserved. Root receives the final commit/tree out of band after normal hooks; do not cherry-pick a merge or re-import prerequisites.

Eight conflicts were resolved by preserving both branches and regenerating contracts/lockfile. A real home poster integration defect was fixed in both mapping paths with failure-first coverage. Main's Core-only transcript writer remains unchanged; actual Studio staged/published/unpublished denial and zero transcript/publication-event creation are proved at that boundary, alongside the original Core restricted-publication behavior. The initial fixture expectation was incorrect, not evidence of exposure.

Both fresh merged and main-ledger upgrade databases finish with 114 source-checksum-matching migrations. All migration history is byte-preserved from both parents. The raw Prisma diff remains nonempty for inherited non-Studio definitions; there are no Studio statements and no diff SQL was applied. Normal Prisma/Pothos/gql.tada generation and seven package types pass.

Focused results: 44 poster cases; 386 Web cases plus one todo; 67 Admin cases and final 19 startup cases; 86 Auth cases; 22 Mastra/native compatibility cases and two actual calendar native cases; two selected transcript database cases (41 unselected skipped). These are scoped checks, not a blanket repository test result. Final changed-source lint passes. See README and raw logs for red/intermediate qualification.

Admin and Web normal builds pass. Admin's isolated retry uses explicit local font CSS responses and passes **both** recommendation retention and Studio calendar workflow verifiers. Web's retry uses dummy required configuration and a private loopback Admin URL, with locale generation and ISR pruning. No production font, API or deployment qualification is inferred. Final manifests and static JS sizes/hashes are recorded; these are not per-page transferred bytes.

Twelve controlled browser component loads preserve exactly one image request: Core through Next's optimizer, Studio through its direct gateway. The additional Core fixture bundle is 164 gzip bytes. Four samples per variant cannot establish full-page latency or close prior inconclusive performance; all measurements and limits remain explicit.

Independent Standards and Spec source reviews report no remaining findings. Documentation wording was corrected; normal hook results accompany the final commit handoff. No further historical suites, media benchmarks or external operations are needed for this bounded merge.

Next root action: verify the delivered commit's exact parents/tree and evidence hashes, then fast-forward the root integration branch. Continue the established review/PR-to-main release process. Configuration remains disabled; no paid batches reopen. Existing runbook gates for actual storage/provider/creative/ElevenLabs/Mux/HTTPS/private transport/cache, deployed execution, Claude app, Lyuba/operator acceptance and performance are unchanged. Published content remains permanently immutable after unpublish; no replacement/republish flow, archive migration or deletion is introduced.
