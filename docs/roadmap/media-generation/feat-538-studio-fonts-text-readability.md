---
id: "feat-538"
title: "Studio font picker and text readability"
owner: "tataihono"
priority: "P1"
status: "complete"
start_date: "2026-09-23"
duration: 3
depends_on: []
blocks: []
tags:
  - "manager"
  - "ai-pipeline"
---

> _This was generated by AI during triage._

## Problem

Text cards lack font selection and shadow/stroke/scrim controls. The designer currently dims the whole video to make text readable.

## Entry Points — Read These First

1. `docs/plans/2026-09-23-studio-designer-feedback.md` — accepted direction, references, and evidence.
2. `apps/manager/src/features/video-studio/inspector.tsx`
3. `packages/studio-contracts/src/index.ts`
4. `packages/studio-contracts/src/contracts.test.ts`
5. `packages/shorts-compositions/src/studio/Composition.tsx`
6. `packages/shorts-compositions/src/fonts.ts`
7. `packages/shorts-compositions/src/devotional/apercu.ts`

## Grep These

- `StudioTimelineItem|studioTextPropertiesSchema|studioTrackSchema`
- `nle-playhead|nle-transport|LivePlayer|StudioComposition`

## What To Build

1. Expose family and weight selectors using vendored fonts (Inter, Montserrat, and Apercu where supported); retain existing values and legacy defaults.
2. Add optional bounded schema fields for text shadow, outline/stroke, and text-local scrim. Provide simple inspector controls and independent off states; existing projects remain visually unchanged.
3. Render scrim behind the text block with suitable padding, not over the entire frame. Support multiline text, alignment, and transform behavior.
4. Load selected font assets in preview and export before relying on metrics. Preserve supported weights and document glyph fallback limits; do not rely on workstation-installed fonts.
5. Check Admin command validation and any schema-derived tool surfaces when extending strict built-in text properties. Read package-local guides; regenerate GraphQL artifacts only if SDL changes.

## Constraints

- Match Lyuba's written instructions and sketch; routine choices are delegated to the agent.
- Preserve existing project data and appearance unless the editor deliberately changes it.
- Do not modify the designer's production project or deploy outside the normal PR flow.
- Read package-local instructions before changing code. No cross-app imports or handwritten generated types.
- Dates/duration are planning estimates, not delivery commitments. Related planning ticket: feat-534.

## Verification

- Browser controls and save/reopen retain font, shadow, scrim, and animated styling. Old properties remain optional.
- Multiline preview and export frames agree for Inter, Montserrat, and Apercu with readability treatments.
- Contract, composition, Manager, and Admin command checks pass; build and load/resource measurements recorded.
- Font errors propagate to preview/render failure. Forced font-network failure was not manually exercised; subset limitations are documented.

Evidence and explicit limits: `docs/validation/studio-feedback/results.md`. Reproduction commands: `docs/validation/studio-feedback/README.md`.
