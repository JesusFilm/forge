---
id: "feat-540"
title: "Studio transitions at adjacent video cuts"
owner: "tataihono"
priority: "P1"
status: "complete"
start_date: "2026-09-23"
duration: 3
depends_on: []
blocks: []
tags:
  - "manager"
  - "ai-pipeline"
---

> _This was generated by AI during triage._

## Problem

The designer requests transitions between video snippets. This is authored behavior, separate from fixing unintended black preview frames at hard cuts.

## Entry Points — Read These First

1. `docs/plans/2026-09-23-studio-designer-feedback.md` — accepted direction, references, and evidence.
2. `apps/manager/src/features/video-studio/timeline.tsx`
3. `apps/manager/src/features/video-studio/inspector.tsx`
4. `packages/studio-contracts/src/index.ts`
5. `packages/shorts-compositions/src/studio/Composition.tsx`
6. `apps/manager/src/services/shorts-browser-preview.ts`

## Grep These

- `StudioTimelineItem|studioTextPropertiesSchema|studioTrackSchema`
- `nle-playhead|nle-transport|LivePlayer|StudioComposition`

## What To Build

1. Offer a discoverable cut control for adjacent video items with None and Crossfade initially, plus bounded duration. Preset choices are agent defaults; existing edits retain hard cuts.
2. Design and document the portable transition representation before implementation, identifying its pair of clips and behavior after trim, move, or deletion.
3. Keep project length and unrelated text/audio timing stable. Explicitly resolve media-handle requirements; show unavailable/limited duration clearly when source handles are insufficient rather than extending media beyond bounds.
4. Use identical frame-based transition behavior in preview and export. Preserve volume behavior unless an audio transition is explicitly requested.
5. Avoid introducing unbounded preview media loading for long projects; keep any preloading proportional to nearby cuts.

## Constraints

- Match Lyuba's written instructions and sketch; routine choices are delegated to the agent.
- Preserve existing project data and appearance unless the editor deliberately changes it.
- Do not modify the designer's production project or deploy outside the normal PR flow.
- Read package-local instructions before changing code. No cross-app imports or handwritten generated types.
- Dates/duration are planning estimates, not delivery commitments. Related planning ticket: feat-534.

## Verification

- Crossfade persists through browser save/reopen; shared tests cover frame progression and cut invalidation after edits.
- Source-handle limits, short clips, nonadjacent and ambiguous pairs are deterministic; retained source ranges include pre-roll.
- Visual crossfade midpoint agrees between preview/export; hard cuts have no sampled decoded gap and export has no blackdetect interval.
- Contract/composition and Manager checks, production build, and bounded preload/bundle measurements pass.

Evidence and explicit limits: `docs/validation/studio-feedback/results.md`. Reproduction commands: `docs/validation/studio-feedback/README.md`.
