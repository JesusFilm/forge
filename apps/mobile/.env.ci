# CI placeholder values — used by GitHub Actions when Doppler is unavailable.
# Same value the code defaults to in a development bundle; kept explicit so a
# CI job that reads the file sees the endpoint it is pinned to.
EXPO_PUBLIC_ADMIN_GRAPHQL_URL=http://localhost:3003/api/graphql
