// Taken off AFTER a line height is scaled or derived from a font size, so each
// line is exactly this much tighter at every screen width. Taken off first, the
// scaling would round it to 2 or 3 depending on the width.
export const LINE_HEIGHT_REDUCTION = 2
