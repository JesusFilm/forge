/**
 * Shared auth UI copy. A leaf module on purpose: component suites mock
 * authActions wholesale, and copy that lived there rendered as undefined.
 */

/** Inline retryable card for a failed hosted sign-in (R2). */
export const SIGN_IN_ERROR_MESSAGE =
  "Something went wrong finishing sign-in. You are not signed in yet — please try again."
