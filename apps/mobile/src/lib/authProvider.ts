// Auth's generic-oauth provider id for the hosted self-RP sign-in. A leaf module
// like authCopy.ts: authActions' suite mocks authSession wholesale, and a
// constant living there reached the call as `undefined` with the suite green.
export const JFP_MOBILE_PROVIDER_ID = "jfp"
