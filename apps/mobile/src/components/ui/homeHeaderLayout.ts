// HomeHeader's action row: its gap below the safe-area inset, and its height.
// HomeLogo centres itself on this row, so the two cannot drift apart.
export const HOME_HEADER_ROW_TOP = 4
export const HOME_HEADER_ROW_HEIGHT = 40
