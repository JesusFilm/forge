-- Unit 4 — drop the Unit 3 spike tables now that the architecture is
-- signed off and real content types (Experience, Video) are in place.
-- See Unit 3 sign-off record in apps/admin/CLAUDE.md.

DROP TABLE IF EXISTS "ping_child";
DROP TABLE IF EXISTS "ping";
