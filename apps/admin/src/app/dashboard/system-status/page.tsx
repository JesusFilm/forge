// Functional placeholder — ops visibility surface for iterating on Core sync
// (Unit 10). The real `systemStatus` GraphQL query + rendered drift-lag
// indicators ship in Unit 12 (design-dependent).

export default function SystemStatusPage() {
  return (
    <main>
      <h1>System status</h1>
      <p>
        Placeholder. The `systemStatus` GraphQL query surfaces per-entity Core
        sync lag (see Unit 10 of the admin-app plan).
      </p>
    </main>
  )
}
