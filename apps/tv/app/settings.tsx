/**
 * /settings route — reached from the Home top bar's Settings tab. Screen options
 * (headerless, background) come from the root Stack in app/_layout.tsx.
 */
export { SettingsScreen as default } from "../src/components/settings/SettingsScreen"
