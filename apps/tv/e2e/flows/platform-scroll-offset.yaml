name: Platform Scroll Offset Differences
platform: [tvos, androidtv]
steps:
  - launch: org.jesusfilm.forgetv
  - wait: 3000
  - dpad: down
  - wait: 300
  - dpad: select
  - wait: 1500
  # Scroll down through sections
  - dpad: down
  - wait: 400
  - dpad: down
  - wait: 400
  - dpad: down
  - wait: 400
  - screenshot: scroll-position
