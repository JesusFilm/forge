name: Home Error State
platform: [tvos, androidtv]
steps:
  - launch: org.jesusfilm.forgetv
  - wait: 5000
  - screenshot: state
  # Try retry if error visible
  - dpad: select
  - wait: 2000
  - screenshot: after-retry
