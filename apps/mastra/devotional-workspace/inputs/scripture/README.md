# Scripture sources

Place supported UTF-8 `.json`, `.md`, `.txt`, `.yaml`, or `.yml` scripture sources here. The migration must populate the public-domain WEB corpus before devotional generation is enabled.
