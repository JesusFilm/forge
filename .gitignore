node_modules/
.pnpm-store/
.turbo/
.expo/
.swc/
.next/
**/.mastra/
next-env.d.ts
dist/
build/
/coverage/
.DS_Store
.env
.env.*
!.env.example
!.env.ci
terraform.tfstate
terraform.tfstate.*
.terraform/
*.tfplan
**/.build/
.gradle/
local.properties
**/tsconfig.tsbuildinfo
.tmp/
**/.tmp/

# Mastra CLI build output (regenerated on each `mastra dev` / `mastra build`)
**/.mastra/

# Playwright / vitest test output (per-run artifacts)
test-results/
**/test-results/

# Local package tarballs (pnpm pack output)
*.tgz

# Xcode user state (do not commit)
**/xcuserdata/
**/*.xcuserstate

# Claude Code local state (machine-specific; commands/ is intentionally tracked)
.claude/settings.local.json
.claude/cache/
.claude/debug/
.claude/telemetry/
.claude/worktrees/
.claude/secrets.local.md
.claude/**/*.dev.md

# Compound engineering working files
.context/
todos/*.md
!todos/.gitkeep
output/
forge.code-workspace
.worktrees

# ce-plan HTML explainers (present-html) — disposable, regenerated per plan
*.explainer.html

# local whisper.cpp install + models (devo snippet detector)
.cache/

# Daily-devotional generated artifacts: rendered videos, downloaded clips, audio,
# manifests, caches, previews. All reproducible output — never committed.
devo/
